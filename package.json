{
  "name": "js-sdk-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client:dev": "pnpm --filter chatdoc-api-demo-client run dev",
    "client:build": "pnpm --filter chatdoc-api-demo-client run build",
    "server:dev": "pnpm --filter chatdoc-api-demo-server run start:dev",
    "start": "concurrently 'pnpm run client:dev' 'pnpm run server:dev'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.3",
    "prettier": "^2.8.8"
  },
  "lint-staged": {
    "projects/**/*.{js,vue}": [
      "eslint --fix",
      "prettier --write"
    ],
    "projects/**/*.{css,scss,vue}": [
      "stylelint --fix",
      "prettier --write"
    ]
  }
}
